import{_ as t,o,c as s,a4 as a}from"./chunks/framework.WdFb_vN1.js";const g=JSON.parse('{"title":"Create Control","description":"","frontmatter":{},"headers":[],"relativePath":"map/dataset/module/CreateControl.md","filePath":"map/dataset/module/CreateControl.md","lastUpdated":1790093896000}'),d={name:"map/dataset/module/CreateControl.md"};function i(r,e,l,n,c,p){return o(),s("div",null,e[0]||(e[0]=[a(`<h1 id="create-control" tabindex="-1">Create Control <a class="header-anchor" href="#create-control" aria-label="Permalink to &quot;Create Control&quot;">​</a></h1><p>Dialog to add a dataset from the UI by <strong>data kind</strong>. <code>LayerControl</code> opens this when <strong>Create layer</strong> is clicked — you usually do <strong>not</strong> mount it yourself.</p><h2 id="props" tabindex="-1">Props <a class="header-anchor" href="#props" aria-label="Permalink to &quot;Props&quot;">​</a></h2><p>Shared map props (<code>mapId</code>, <code>position</code>, <code>controlLayout</code>, <code>controlVisible</code>, …) apply. CreateControl also registers a <strong>chrome button</strong> (<code>mdiPlus</code>) via ModuleContainer <code>#btn</code> / toolbar.</p><table tabindex="0"><thead><tr><th>Prop</th><th>Type</th><th>Default</th><th>Effect</th></tr></thead><tbody><tr><td><code>show</code> / <code>v-model:show</code></td><td><code>boolean</code></td><td>—</td><td>Open the create popup</td></tr><tr><td><code>createLayerTypes</code></td><td><code>LayerType[]</code></td><td>all <code>LAYER_TYPES</code></td><td>Allowlist for the type select (<code>resolveCreateControlLayerTypes</code>)</td></tr><tr><td><code>controlVisible</code></td><td><code>boolean</code></td><td><code>true</code></td><td>Show map chrome button; LayerControl nests with <code>false</code></td></tr></tbody></table><p>Standalone:</p><div class="language-vue vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">vue</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">CreateControl</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> v-model</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">show</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">show</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> :</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">create-layer-types</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;geojson&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;xyz&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> /&gt;</span></span></code></pre></div><p>When nested under LayerControl, the host passes <code>controlVisible={false}</code> and <code>createLayerTypes</code> so only LayerControl’s <strong>+</strong> opens create.</p><p>Layer types (<code>LAYER_TYPES</code>):</p><table tabindex="0"><thead><tr><th>Type</th><th>Data</th></tr></thead><tbody><tr><td><code>geojson</code></td><td>GeoJSON / GIS files (worker <code>geojson</code>)</td></tr><tr><td><code>filegdb</code></td><td>ESRI File Geodatabase — <code>.gdb.zip</code> / <code>*_gdb.zip</code> / <code>.gdb</code> folder (optional peer <code>gdal3.js</code>)</td></tr><tr><td><code>xyz</code></td><td>XYZ URL template — raster or vector (<code>.pbf</code>) via <code>tileKind</code> / URL sniff</td></tr><tr><td><code>tilejson</code></td><td>TileJSON URL (<code>tiles.json</code>) — <code>tiles[]</code> + <code>vector_layers</code> metadata</td></tr><tr><td><code>mbtiles</code></td><td>Local <code>.mbtiles</code> — vector or raster from metadata (<code>format</code> / <code>vector_layers</code>)</td></tr><tr><td><code>pmtiles</code></td><td><code>.pmtiles</code> file or URL — vector or raster from header <code>tileType</code></td></tr></tbody></table><p>Optional peers for archives: <code>pmtiles</code>, <code>sql.js</code> (same pattern as GIS peers below).</p><h2 id="samples" tabindex="-1">Samples <a class="header-anchor" href="#samples" aria-label="Permalink to &quot;Samples&quot;">​</a></h2><p>Built-in samples are <code>CreateControlSample</code> objects keyed by <strong><code>layerKind</code></strong> only (<code>id</code>, <code>label</code>, <code>layerKind</code>, <code>config</code>, optional <code>dataUrl</code>). There is no separate <code>dataFormat</code> field — vector vs raster XYZ is carried in <code>config.tileKind</code>.</p><table tabindex="0"><thead><tr><th>Kind</th><th>Constant</th><th>Package entry</th></tr></thead><tbody><tr><td><code>geojson</code></td><td><code>VECTOR_SAMPLES</code></td><td><code>@hungpvq/map-dataset/vector-tile</code></td></tr><tr><td><code>xyz</code></td><td><code>RASTER_XYZ_SAMPLES</code> + <code>VECTOR_TILE_SAMPLES</code></td><td><code>/raster</code> + <code>/vector-tile</code></td></tr><tr><td><code>tilejson</code></td><td><code>TILEJSON_SAMPLES</code> (MapLibre <a href="https://demotiles.maplibre.org/tiles/tiles.json" target="_blank" rel="noreferrer">demotiles tiles.json</a>)</td><td><code>/vector-tile</code></td></tr><tr><td><code>mbtiles</code> / <code>pmtiles</code></td><td>none</td><td>—</td></tr><tr><td><code>filegdb</code></td><td>none (upload only)</td><td>—</td></tr></tbody></table><p>Helpers: <code>getCreateControlSamples(layerKind)</code>, <code>applyCreateControlSample(sample)</code>, <code>loadCreateControlTileJsonFromUrl</code> / <code>tileJsonToCreateControlPatch</code>.</p><h2 id="filegdb" tabindex="-1">FileGDB <a class="header-anchor" href="#filegdb" aria-label="Permalink to &quot;FileGDB&quot;">​</a></h2><p><code>LAYER_TYPES.filegdb</code> / <code>ConfigFilegdbHelper</code> accepts a <strong><code>.gdb.zip</code></strong>, <strong><code>*_gdb.zip</code></strong>, or a <strong><code>.gdb</code> folder</strong> (drop or “Choose folder”). Parsing uses optional peer <strong><code>gdal3.js</code></strong> (OpenFileGDB + <code>jszip</code> for archives), keeps only feature classes with drawable geometries, and converts each to GeoJSON. Create uses <strong><code>createGeoJsonLayersDataset</code></strong> (same list shape as MBTiles vector):</p><ul><li><strong>1 feature class</strong> → one flat list row (<code>createGeoJsonDataset</code>)</li><li><strong>2+ feature classes</strong> → one parent GroupSubList (master show + <strong>fill bound</strong>) and each class as a SubList</li><li><strong>Settings</strong> → source-layer checkboxes (like MBTiles); <strong>no</strong> style-type or color picker (paint is always auto: area(+outline) + line + point; each class gets <code>getChartColorAt(index)</code>)</li><li>Under each checkbox: <strong>feature count</strong>, <strong>geometry types</strong>, and <strong>fields</strong> (inferred from properties) — same chips pattern as TileJSON / MBTiles</li><li>Large classes (<strong>&gt; 5 000</strong> features) start <strong>unchecked</strong> (avoid browser OOM); at least one layer stays enabled</li><li><strong>Fill bound</strong> → parent + each child from GeoJSON bbox (async with sync fallback)</li></ul><p><strong>gdal3.js load (browser, main thread only):</strong> never Vite-<code>import</code> the package (Emscripten <code>CModule().then</code> breaks). FileGDB parse stays on the <strong>main thread</strong> via <code>loadGisFileAsync</code> (WorkerMonitor id <code>geojson</code>, engine <code>main</code>) so the GIS worker graph never loads gdal. Default assets come from jsDelivr CDN; override with <code>configureFileGdbGdal({ path: &#39;/gdal&#39; })</code> and/or copy <code>node_modules/gdal3.js/dist/package/*</code> into static hosting. Main thread uses a classic <code>&lt;script&gt;</code> UMD; gdal’s nested worker stays off (<code>useWorker: false</code>). Exclude <code>gdal3.js</code> from Vite <code>optimizeDeps</code> (<code>mapDatasetGisWorker()</code> or demos).</p><h2 id="vector-tilejson-archives" tabindex="-1">Vector TileJSON / archives <a class="header-anchor" href="#vector-tilejson-archives" aria-label="Permalink to &quot;Vector TileJSON / archives&quot;">​</a></h2><p>CreateControl lists each <code>vector_layers</code> id with a <strong>checkbox</strong> (default all on).</p><p>Created dataset list structure:</p><ul><li><strong>1 enabled source-layer</strong> → one flat list row (same pattern as GeoJSON / raster)</li><li><strong>2+ enabled source-layers</strong> → <strong>one parent GroupSubList</strong> (master show checkbox) and each layer as a <strong>SubList</strong> with TileServer-style paint (fill+outline, line, point) and a distinct chart color. Children start <strong>collapsed</strong> (<code>init_show_children: false</code>).</li></ul><p>When TileJSON / MBTiles / PMTiles metadata includes <code>vector_layers.fields</code> and/or geometry hints (<code>tilestats</code>), CreateControl shows those under each source-layer checkbox. <strong>FileGDB</strong> shows the same chip pattern from the converted GeoJSON (count, geometry types, fields).</p><p><strong>TileJSON</strong> (<code>ConfigTilejsonHelper</code>): paste/load a document URL → resolve relative <code>tiles[]</code> against that URL (braces <code>{z}/{x}/{y}</code> kept) → create with <code>createVectorTileDataset</code> (HTTP(S) templates, no archive protocol).</p><p><strong>Local archive protocols</strong> (after <code>ensureVectorTileProtocols</code>): MapLibre loads tiles via custom schemes — <code>mbtiles-local://{archiveId}/{z}/{x}/{y}</code> and <code>pmtiles-local://{archiveId}/{z}/{x}/{y}</code> — both resolve through the archive registry + <code>getVectorTileArchiveTile</code> (not the official <code>pmtiles.Protocol</code> scheme). Raster archives use the same URLs with <code>createRasterUrlDataset</code>.</p><p>After TileJSON load or archive upload (or PMTiles URL load), CreateControl shows a <strong>metadata</strong> card (vector vs raster, format, zoom, bounds, layer count) via <code>buildCreateControlArchiveMetaChips</code>.</p><p><strong>Area style:</strong> create paths expand <code>area</code> into fill + outline line (QGIS-like regions).</p><p>Deprecated draft keys <code>vector</code> / <code>rasterxyz</code> / <code>vectortile</code> / <code>raster</code> are normalized via <code>normalizeLayerType</code> (<code>vectortile</code> → <code>xyz</code>).</p><p>GeoJSON / KML / GPX / Shapefile file read, parse, and CRS reproject run in a <a href="./../worker.html">Web Worker</a>. <strong>FileGDB</strong> parse stays on the main thread (gdal3.js). Configure Vite <code>worker.format: &#39;es&#39;</code> (and <code>nxViteTsPaths</code> on <code>worker.plugins</code> in this Nx workspace) or large non-FileGDB files fall back to the main thread and can freeze the UI. Mount <a href="/docs/map/core/module/WorkerControl.html">WorkerControl</a> to watch progress and errors.</p><h2 id="optional-gis-peers" tabindex="-1">Optional GIS peers <a class="header-anchor" href="#optional-gis-peers" aria-label="Permalink to &quot;Optional GIS peers&quot;">​</a></h2><p><code>@hungpvq/map-dataset</code> does <strong>not</strong> bundle GIS format parsers. Install them in the app when using CreateControl / file import:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> i</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> shpjs</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> papaparse</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> jszip</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> topojson-client</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> @tmcw/togeojson</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> @xmldom/xmldom</span></span></code></pre></div><p>For FileGDB:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> i</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> gdal3.js</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> jszip</span></span></code></pre></div><p>For MBTiles / PMTiles:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> i</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> sql.js</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> pmtiles</span></span></code></pre></div><table tabindex="0"><thead><tr><th>Format</th><th>Peer(s)</th></tr></thead><tbody><tr><td>GeoJSON / GeoJSONL / WKT</td><td>none (<code>parseGisText</code> sync)</td></tr><tr><td>CSV</td><td><code>papaparse</code></td></tr><tr><td>KML / GPX</td><td><code>@tmcw/togeojson</code>, <code>@xmldom/xmldom</code></td></tr><tr><td>TopoJSON</td><td><code>topojson-client</code></td></tr><tr><td>ZIP / KMZ</td><td><code>jszip</code> (+ KML peers for KMZ)</td></tr><tr><td>Shapefile</td><td><code>shpjs</code></td></tr><tr><td>FileGDB (<code>.gdb.zip</code> / folder)</td><td><code>gdal3.js</code> (+ <code>jszip</code> for archives)</td></tr><tr><td>MBTiles</td><td><code>sql.js</code></td></tr><tr><td>PMTiles</td><td><code>pmtiles</code></td></tr><tr><td>TileJSON</td><td>none (fetch JSON)</td></tr></tbody></table><p>Programmatic parse: <code>parseGisTextAsync</code> / <code>loadGis*Async</code> from <code>@hungpvq/map-dataset/create-control</code> — see <a href="./../worker.html">GIS worker</a>.</p><h2 id="ux-notes" tabindex="-1">UX notes <a class="header-anchor" href="#ux-notes" aria-label="Permalink to &quot;UX notes&quot;">​</a></h2><ul><li>The dialog keeps a small draft in <code>sessionStorage</code> (type, name, CRS) via <code>loadCreateControlDraft</code> / <code>saveCreateControlDraft</code>.</li><li>If loaded file CRS differs from selected CRS, UI warns that data will be reprojected.</li><li>Multi-file upload accepts one GIS file, or one shapefile set (<code>.shp</code> + sidecars / <code>.zip</code>).</li><li><strong>Folder drop:</strong> dropping a directory walks entries (<code>collectFilesFromDataTransfer</code>) and keeps GIS extensions only.</li><li><strong>OS paste:</strong> on the File tab, paste clipboard files as upload, or paste text to switch to Raw and parse (<code>readClipboardGisPaste</code> / <code>parseCreateControlPastedText</code>).</li></ul><h2 id="implementation-adapters" tabindex="-1">Implementation (adapters) <a class="header-anchor" href="#implementation-adapters" aria-label="Permalink to &quot;Implementation (adapters)&quot;">​</a></h2><p>Create / validate / defaults live on core <strong><code>LayerHelper</code></strong> from <code>@hungpvq/map-dataset/create-control</code>. Vue and React adapters only supply framework form UI as <strong>leaf modules</strong>.</p><table tabindex="0"><thead><tr><th></th><th>Import</th></tr></thead><tbody><tr><td>Protocol</td><td><code>LayerHelper</code>, <code>LAYER_TYPES</code>, <code>ConfigTilejsonHelper</code>, <code>ConfigFilegdbHelper</code>, parse/upload helpers from <code>@hungpvq/map-dataset/create-control</code></td></tr><tr><td>Vue forms</td><td><code>CreateControl/config/*.vue</code> leaf SFCs (<code>filegdb-upload.vue</code>, <code>filegdb-settings.vue</code>, <code>tilejson-json.vue</code>, <code>archive-settings.vue</code>, …)</td></tr><tr><td>React forms</td><td><code>CreateControl/config/*.tsx</code> leaf modules (<code>filegdb-upload.tsx</code>, <code>filegdb-settings.tsx</code>, …)</td></tr></tbody></table><h2 id="props-1" tabindex="-1">Props <a class="header-anchor" href="#props-1" aria-label="Permalink to &quot;Props&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Prop</th><th>Description</th><th>Type</th><th>Required</th><th>Default Value</th></tr></thead><tbody><tr><td><code>mapId</code></td><td></td><td><code>string</code></td><td><code>false</code></td><td>--</td></tr><tr><td><code>dragId</code></td><td></td><td><code>string</code></td><td><code>false</code></td><td>--</td></tr><tr><td><code>btnWidth</code></td><td></td><td><code>number</code></td><td><code>false</code></td><td>40</td></tr><tr><td><code>controlOrder</code></td><td>CSS flex <code>order</code> for the standalone button</td><td><code>number</code></td><td><code>false</code></td><td>0</td></tr><tr><td><code>position</code></td><td></td><td><code>&#39;top-left&#39;</code>, <code>&#39;top-right&#39;</code>, <code>&#39;bottom-left&#39;</code>, <code>&#39;bottom-right&#39;</code></td><td><code>false</code></td><td><code>&#39;bottom-right&#39;</code></td></tr><tr><td><code>controlLayout</code></td><td><code>&#39;standalone&#39;</code> (corner), <code>&#39;toolbar&#39;</code> (always in toolbar), <code>&#39;button&#39;</code> (always corner; not auto-promoted on mobile)</td><td><code>&#39;standalone&#39; | &#39;toolbar&#39; | &#39;button&#39;</code></td><td><code>false</code></td><td><code>&#39;standalone&#39;</code></td></tr><tr><td><code>controlVisible</code></td><td></td><td><code>boolean</code></td><td><code>false</code></td><td><code>true</code></td></tr></tbody></table><p><code>Map</code> also accepts <code>buttonInMobile</code> (<code>&#39;button&#39;</code> | <code>&#39;toolbar&#39;</code> | <code>&#39;menu&#39;</code>, default <code>&#39;button&#39;</code>). On viewports <strong>≤640px</strong>:</p><ul><li><code>&#39;button&#39;</code> — leave corner buttons unchanged</li><li><code>&#39;toolbar&#39;</code> — move declared control buttons into a single <code>ToolbarControl</code> host (except <code>controlLayout=&quot;button&quot;</code>). Mount one <code>ToolbarControl</code> in the map slot</li><li><code>&#39;menu&#39;</code> — hide per-control corner buttons; fan out intact clusters by <code>position</code> into corner stacks with outside-in More. Mount one <code>ToolbarControl</code>.</li></ul><p>See <a href="/docs/map/core/module/ToolbarControl.html">ToolbarControl</a>.</p><table tabindex="0"><thead><tr><th>Prop</th><th>Type</th><th>Required</th><th>Effect</th></tr></thead><tbody><tr><td><code>show</code></td><td><code>boolean</code></td><td>yes</td><td>Open the dialog</td></tr></tbody></table><h2 id="events" tabindex="-1">Events <a class="header-anchor" href="#events" aria-label="Permalink to &quot;Events&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Name</th><th>Payload</th><th>Framework</th></tr></thead><tbody><tr><td><code>update:show</code></td><td><code>boolean</code></td><td>Vue (<code>v-model:show</code>)</td></tr><tr><td><code>onShowChange</code></td><td><code>(show: boolean) =&gt; void</code></td><td>React</td></tr></tbody></table><h2 id="vue" tabindex="-1">Vue <a class="header-anchor" href="#vue" aria-label="Permalink to &quot;Vue&quot;">​</a></h2><div class="language-vue vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">vue</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">script</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> setup</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> lang</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;ts&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { ref } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;vue&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { Map } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@hungpvq/vue-map-core&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { CreateControl } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@hungpvq/vue-map-dataset&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@hungpvq/map-core/style.css&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@hungpvq/map-dataset/style.css&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@hungpvq/vue-map-core/style.css&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@hungpvq/vue-map-dataset/style.css&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@hungpvq/vue-draggable/style.css&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;/</span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">script</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;</span></span></code></pre></div><p>See demo apps for a full mount example.</p><h2 id="semver" tabindex="-1">SemVer <a class="header-anchor" href="#semver" aria-label="Permalink to &quot;SemVer&quot;">​</a></h2><ul><li>Renaming CreateControl <code>LayerType</code> keys (<code>vector</code> → <code>geojson</code>, …) is a <strong>breaking</strong> change for consumers that hard-code those strings. Runtime aliases via <code>normalizeLayerType</code> soften drafts; treat a published bump as <strong>major</strong> when releasing this contract.</li><li>Adding <code>tilejson</code> / <code>TILEJSON_SAMPLES</code> / <code>ConfigTilejsonHelper</code> is <strong>minor</strong>.</li><li>Adding <code>filegdb</code> / <code>ConfigFilegdbHelper</code> / optional peer <code>gdal3.js</code> is <strong>minor</strong>.</li><li>Removing optional <code>CreateControlSample.dataFormat</code> (use <code>layerKind</code> + <code>config.tileKind</code>) is <strong>minor</strong>.</li></ul>`,57)]))}const k=t(d,[["render",i]]);export{g as __pageData,k as default};
