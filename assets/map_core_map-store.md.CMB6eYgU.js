import{_ as t,o as a,c as s,a4 as o}from"./chunks/framework.WdFb_vN1.js";const g=JSON.parse('{"title":"Map store & getMap","description":"","frontmatter":{},"headers":[],"relativePath":"map/core/map-store.md","filePath":"map/core/map-store.md","lastUpdated":1789700157000}'),d={name:"map/core/map-store.md"};function i(r,e,n,c,p,l){return a(),s("div",null,e[0]||(e[0]=[o(`<h1 id="map-store-getmap" tabindex="-1">Map store &amp; <code>getMap</code> <a class="header-anchor" href="#map-store-getmap" aria-label="Permalink to &quot;Map store &amp; \`getMap\`&quot;">​</a></h1><p>Each <strong><code>mapId</code></strong> holds <strong>one</strong> MapLibre instance at a time (one shell / one live map). Reusing an id after <code>removeMap</code> is allowed via a new <code>initMap</code>; calling <code>initMap</code> while another live instance already owns that id throws <code>MapInitializationError</code>.</p><h2 id="who-exports-what" tabindex="-1">Who exports what <a class="header-anchor" href="#who-exports-what" aria-label="Permalink to &quot;Who exports what&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Concern</th><th>Package / entry</th></tr></thead><tbody><tr><td><code>getMap</code>, <code>subscribeMapReady</code>, <code>registerMapAccessor</code>, <code>registerMapReadySubscriber</code>, <code>registerMapStoreCleanup</code>, <code>MAP_PLATFORM_HOST</code>, <code>listMapPlatformHosts</code>, <code>MAP_PLATFORM_REGISTRY_METHOD</code>, <code>MapStoreManager</code>, <code>MAP_STORE_KEY</code>, <code>hasMapInstance</code></td><td><code>@hungpvq/map-core</code></td></tr><tr><td>Domain features (basemap, crs, event, image, legend, measurement, menu, print, theme, toolbar)</td><td><code>@hungpvq/map-core/&lt;domain&gt;</code></td></tr><tr><td>In-worker helpers</td><td><code>@hungpvq/map-core/worker</code></td></tr><tr><td><code>createMapScopedStore</code>, <code>destroyMapScopedStore</code>, <code>getStore</code>, <code>addStore</code>, <code>useMapContainer</code></td><td><code>@hungpvq/vue-map-core</code> / <code>@hungpvq/react-map-core</code></td></tr></tbody></table><p>Adapters <strong>do not</strong> re-export <code>getMap</code> or domain protocol. Apps import platform APIs from <code>@hungpvq/map-core</code> and feature APIs from the matching subpath (see <a href="./stable-api.html">Stable API</a>).</p><h2 id="access-the-map" tabindex="-1">Access the map <a class="header-anchor" href="#access-the-map" aria-label="Permalink to &quot;Access the map&quot;">​</a></h2><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { getMap, subscribeMapReady } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@hungpvq/map-core&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> type</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { MapSimple } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@hungpvq/map-core&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> map</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> getMap</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;map-1&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">); </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// MapSimple | undefined</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">getMap</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;map-1&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, (</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">map</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> MapSimple</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  // Runs now if ready, or when MapStoreManager emits READY</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// Prefer when you need to cancel the wait (unmount / destroy):</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> unsubscribe</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> subscribeMapReady</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;map-1&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, (</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">map</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  // …</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">unsubscribe</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span></code></pre></div><p>The Vue / React <code>Map</code> shell registers the accessor via <code>registerMapAccessor</code> / <code>registerMapReadySubscriber</code> and calls <code>initMap</code> / <code>removeMap</code> through <code>useMapContainer</code>.</p><p>Platform accessors are stored as <strong>UniversalRegistry global methods</strong> under reserved keys (<code>MAP_PLATFORM_REGISTRY_METHOD.*</code>, e.g. <code>__platform.getMap</code>). They live in the shared <code>map:registry:global</code> bag — <code>clearMap</code> / <code>removeMap</code> does <strong>not</strong> remove them — so duplicate package copies still share one wiring.</p><p><strong><code>registerMapAccessor</code> / <code>registerMapReadySubscriber</code> / <code>registerMapStoreCleanupRegistrar</code>:</strong> accept optional <code>{ hostId }</code> (e.g. <code>MAP_PLATFORM_HOST.VUE_MAP_CORE</code> / <code>REACT_MAP_CORE</code>). Same <code>hostId</code> replaces that host only (version bump); different hosts <strong>coexist</strong> — composite <code>getMap</code> / READY fan out until a map is found. Returns <code>{ hostId, version, unregister }</code>. Prefer one framework host per app; multi-host pages no longer silently last-writer-wins.</p><h2 id="scoped-stores" tabindex="-1">Scoped stores <a class="header-anchor" href="#scoped-stores" aria-label="Permalink to &quot;Scoped stores&quot;">​</a></h2><p>Use documented <code>MAP_STORE_KEY</code> values for feature state keyed by <code>mapId</code>:</p><table tabindex="0"><thead><tr><th>Key</th><th>Value</th><th>Typical use</th></tr></thead><tbody><tr><td><code>MITT</code></td><td><code>mitt</code></td><td>Event bus</td></tr><tr><td><code>EVENT</code></td><td><code>event</code></td><td>Map event management</td></tr><tr><td><code>IMAGE</code></td><td><code>image</code></td><td>Map images</td></tr><tr><td><code>TOOLBAR</code></td><td><code>toolbar</code></td><td>Toolbar modules</td></tr><tr><td><code>LANG</code></td><td><code>lang</code></td><td>Locale catalogs + active language (<code>registerLocale</code> / <code>setLanguage</code>)</td></tr><tr><td><code>CRS</code></td><td><code>crs</code></td><td>CRS store</td></tr><tr><td><code>PRINT</code></td><td><code>print</code></td><td>Print options</td></tr><tr><td><code>REGISTRY</code></td><td><code>registry</code></td><td>Control registry scope</td></tr><tr><td><code>BASEMAP</code></td><td><code>basemap</code></td><td>Basemap selection</td></tr></tbody></table><p>Adapter domains may also use <strong>string</strong> scoped keys outside <code>MAP_STORE_KEY</code>:</p><table tabindex="0"><thead><tr><th>Key</th><th>Owner</th><th>Constant</th></tr></thead><tbody><tr><td><code>&#39;dataset&#39;</code></td><td>vue/react <code>map-dataset</code> store bag</td><td><code>MAP_DATASET_STORE_KEY</code> from <code>@hungpvq/map-dataset</code></td></tr><tr><td><code>&#39;draw&#39;</code></td><td>vue/react <code>map-draw</code> session config</td><td>(adapter-local; prefer a named constant when touching)</td></tr></tbody></table><p>Prefer <code>MAP_STORE_KEY</code> when adding new <strong>core-owned</strong> stores; domain keys live in the owning package. Changing a documented store key <strong>string value</strong> is a SemVer <strong>major</strong>.</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { MAP_STORE_KEY } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@hungpvq/map-core&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { createMapScopedStore, getStore } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@hungpvq/vue-map-core&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// or from &#39;@hungpvq/react-map-core&#39;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">createMapScopedStore</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(mapId, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">MAP_STORE_KEY</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">CRS</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, () </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> ({ </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">/* … */</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }));</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> crs</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> getStore</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(mapId, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">MAP_STORE_KEY</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">CRS</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span></code></pre></div><p><strong>Cleanup callbacks</strong> passed to <code>createMapScopedStore(..., { cleanup })</code> must resolve the store with <code>getStore(mapId, key)</code> (or an already-captured reference). Do <strong>not</strong> call the same <code>useMap*Store(mapId)</code> factory from inside <code>cleanup</code> — that creates a circular TypeScript inference (<code>TS7023</code>) and can re-enter <code>addStore</code> during teardown.</p><p>Domain packages (dataset, draw, event, highlight, …) should also register teardown with <code>registerMapStoreCleanup(mapId, key, fn)</code> when their resources are not owned by a scoped-store <code>cleanup</code> option.</p><p>Changing a <code>MAP_STORE_KEY.*</code> <strong>string value</strong> is a SemVer <strong>major</strong>.</p><h2 id="process-wide-singletons" tabindex="-1">Process-wide singletons <a class="header-anchor" href="#process-wide-singletons" aria-label="Permalink to &quot;Process-wide singletons&quot;">​</a></h2><p>These keys live on <code>@hungpvq/shared-store</code> (<code>globalThis.$_hungpv_store</code>) unless noted. Duplicate package copies and Vue/React adapters must share them — do <strong>not</strong> invent parallel bags or class-static <code>Map</code>s.</p><table tabindex="0"><thead><tr><th>Key / export</th><th>Kind</th><th>Purpose</th></tr></thead><tbody><tr><td><code>__hungpvq_map_errorHandler__</code> (<code>errorHandler</code>)</td><td><code>getOrCreateStore</code></td><td>Centralized map error handler singleton</td></tr><tr><td><code>__hungpvq_gis_worker__</code></td><td><code>getOrCreateStore</code></td><td>GIS Web Worker URL override (<code>configureGisWorker</code>)</td></tr><tr><td><code>map:registry:global</code></td><td><code>getOrCreateStore</code></td><td>UniversalRegistry global methods / components / menu handlers</td></tr><tr><td><code>map:registry:maps</code></td><td><code>getOrCreateStore</code></td><td>Per-<code>mapId</code> registry bags</td></tr><tr><td><code>map:registry:controls</code></td><td><code>getOrCreateStore</code></td><td>Control handle registry</td></tr><tr><td><code>hungpvq.map-theme-mode</code> (+ optional <code>:&lt;mapId&gt;</code>)</td><td><code>localStorage</code> (<code>MAP_THEME_STORAGE_KEY</code> / <code>getMapThemeStorageKey(mapId)</code>)</td><td>Theme preference. Default <strong>process-global</strong>; <code>ThemeControl scope=&quot;map&quot;</code> uses per-map key.</td></tr><tr><td><code>map:core</code></td><td><code>defineStore</code> / root bag</td><td>Per-<code>mapId</code> map store entries (instance, scoped features, cleanups)</td></tr><tr><td><code>map:core:meta</code></td><td><code>getOrCreateStore</code></td><td><code>removedMapIds</code> tombstones after <code>removeMap</code></td></tr></tbody></table><p>Related process pins outside this table: <code>__hungpvq_map_errorCapture__</code>, React <code>__hungpvq_react_map_storeManager__</code>, and <code>LoggerFactory</code> on <code>@hungpvq/shared-log</code>’s own <code>globalThis</code> key.</p><h2 id="multi-map-dom-notes" tabindex="-1">Multi-map DOM notes <a class="header-anchor" href="#multi-map-dom-notes" aria-label="Permalink to &quot;Multi-map DOM notes&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Surface</th><th>Scope</th></tr></thead><tbody><tr><td>Theme (<code>applyMapTheme</code> → <code>html</code> and/or map shell)</td><td><code>document</code> (default) = process-global; <code>map</code> = per-<code>mapId</code></td></tr><tr><td>Layer search (<code>/</code> shortcut)</td><td>Per <code>mapId</code> via <code>[data-map-layer-search][data-map-id]</code></td></tr><tr><td>Devtools drag host</td><td><code>containerId</code> and/or <code>mapId</code> — no first-match in document</td></tr></tbody></table><h3 id="multi-map-caveats-apps-with-map-a-map-b" tabindex="-1">Multi-map caveats (apps with Map A + Map B) <a class="header-anchor" href="#multi-map-caveats-apps-with-map-a-map-b" aria-label="Permalink to &quot;Multi-map caveats (apps with Map A + Map B)&quot;">​</a></h3><ul><li><strong>Per-map state is safe</strong> when keyed by <code>mapId</code> (<code>MapStoreManager</code>, scoped stores, registry maps bag).</li><li><strong>Theme:</strong> default <code>ThemeControl</code> / <code>bootstrapMapTheme</code> use <code>scope: &#39;document&#39;</code> (one <code>html</code> chrome theme). For independent themes use <code>&lt;ThemeControl scope=&quot;map&quot; /&gt;</code> or <code>applyMapTheme(resolved, { scope: &#39;map&#39;, mapId })</code> / <code>bootstrapMapTheme(mode, { scope: &#39;map&#39;, mapId })</code>.</li><li>Platform accessors are <strong>multi-host</strong> (<code>hostId</code>); Vue/React register separately and composite <code>getMap</code> resolves across hosts.</li><li>Prefer <code>subscribeMapReady(mapId, cb)</code> over fire-and-forget <code>getMap(id, cb)</code> so each shell can unsubscribe on unmount without racing another map’s READY.</li><li><strong>Consumer rule:</strong> call <code>bootstrapMapTheme</code> once for document chrome (or omit and let ThemeControl apply); use <code>scope=&quot;map&quot;</code> when maps must differ.</li></ul><h2 id="lifecycle-engine" tabindex="-1">Lifecycle (engine) <a class="header-anchor" href="#lifecycle-engine" aria-label="Permalink to &quot;Lifecycle (engine)&quot;">​</a></h2><p><code>MapStoreManager</code> (used by adapters):</p><ul><li><code>initMap(mapId, map)</code> — set the single instance and emit <code>MAP_CORE_EVENT.READY</code> (fails if a different live map already owns <code>mapId</code>); clears the process-wide tombstone for that id</li><li><code>removeMap(mapId)</code> — run registered cleanups, clear registry scope, delete the store entry, and tombstone the id so <code>getMap(id, cb)</code> / <code>subscribeMapReady</code> do <strong>not</strong> wait for READY or recreate an entry</li><li><code>getMap(mapId, cb?)</code> — <code>MapSimple | undefined</code>; with <code>cb</code>, waits for READY only when the id is not tombstoned (internally shares wait logic with <code>subscribeMapReady</code>)</li><li><code>subscribeMapReady(mapId, cb)</code> — <code>() =&gt; void</code> unsubscribe; sync when live, waits when pending, no-op when tombstoned</li><li><code>registerCleanup</code> / public <code>registerMapStoreCleanup(mapId, key, fn)</code> — map-scoped teardown on <code>removeMap</code></li></ul><p><strong>Tombstones</strong> live in a process-wide <code>@hungpvq/shared-store</code> bag (<code>map:core:meta.removedMapIds</code>), so separate Vue and React <code>MapStoreManager</code> instances (or duplicate package copies) share the same removed-id set.</p><p>See also <a href="./stable-api.html">Stable API</a> · <a href="./error-handling.html">Error handling</a> · <a href="./minimal-starter.html">Minimal starter</a>.</p>`,33)]))}const k=t(d,[["render",i]]);export{g as __pageData,k as default};
